package com.sportspredictor.mcpserver;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/** Sports Predictor MCP server application entry point. */
@SpringBootApplication
public class SportsPredictorMcpServerApplication {

    /** Starts the Spring Boot application. */
    public static void main(String[] args) {
        SpringApplication.run(SportsPredictorMcpServerApplication.class, args);
    }
}
